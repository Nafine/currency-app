apiVersion: v1
kind: Service
metadata:
  name: {{ include "yadro-app.fullname" . }}
  labels:
    {{- include "yadro-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yadro-app.selectorLabels" . | nindent 4 }}
